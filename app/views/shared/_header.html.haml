%navbar.navbar-fixed-top
  %h1.navbar-link.pull-left
    = link_to "Dinner Dash", home_path
  %h1.navbar-link.pull-right
    - if current_user
      = "You're signed in as: #{current_user.email}"
      = link_to " Sign out", sign_out_path
    - else
      = link_to "Sign in ", sign_in_path
